/* 删除所有值在s和t之间的t中间的值 ,如果s或t不合理或者顺序表为空,显示错误信息退出运行*/
struct SqList
{
    int data[N];
    int length;
}

void delete_from_s_to_t(SqList* L,int s,int t)
{
    if(s<=0||t<=0||s>=t||L->length<=0)
    {
        return false;
    }

    
}
